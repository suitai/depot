Directory:
  - Test/7
  - Stable/7
  - Tool/7
  - Documents
  - Pictures

Operate:
  Upload:
    -
      rename: ${dest}/${filename}
    -
      match: .+\.rpm
      exec: createrepo ${dirname}/..
  Remove:
    -
      unlink: ${filepath}
    -
      match: .+\.rpm
      exec: createrepo ${dirname}/..

List:
  ignore:
    - repodata
