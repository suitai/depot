Directory:
  - Test/7
  - Stable/7
  - Tool/7
  - Documents
  - Pictures

Operate:
  Upload:
    -
      match: .+\.rpm
      rename: ${dest}/${filename}
      post: createrepo ${dirname}/..
    -
      rename: ${dest}/${filename}
  Remove:
    -
      match: .+\.rpm
      post: createrepo ${dirname}/..

List:
  exclude:
    - repodata
