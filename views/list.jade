table.table
  thead
    tr
      th(scope="col") #
      th(scope="col") Path
  tbody
    - for (var i = 0; i < list.length; i++) {
    tr
      th(scope="col") #{i + 1}
      th(scope="col") #{list[i]}
    - }
