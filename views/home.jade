p.title.font-weight-bold.mt-3 Welcome to #{title}

a(href=download) Downloads

p.mt-3 Download yum.repo
pre
  kbd curl --basic -u <i>username</i>:<i>password</i> #{url}etc/yum.repo | sudo tee /etc/yum.repos.d/#{title}.repo

p.mt-3 Upload files with curl
pre
  kbd curl --basic -u <i>username</i>:<i>password</i> -F "dest=Test" -F "file=@filename" #{url}upload
